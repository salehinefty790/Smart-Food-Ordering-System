# Smart-Food-Ordering-System
mart Food Ordering System* is a Java-based program that lets users order food, calculate bills, and apply discounts. It demonstrates key OOP concepts like encapsulation, inheritance, polymorphism, and abstraction.
